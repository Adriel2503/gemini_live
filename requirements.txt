# NOTA: la fuente de verdad de dependencias es pyproject.toml + uv.lock.
# El Docker build y `uv sync` usan ESOS, no este archivo. requirements.txt
# se mantiene solo como fallback para `pip install -r`. Si cambias una
# dependencia, hazlo en pyproject.toml y corre `uv lock`.
typer>=0.12,<1.0
rich>=13.7,<14.0
python-dotenv>=1.0,<2.0
numpy>=1.26,<3.0
sounddevice>=0.4.7,<1.0
scipy>=1.11,<2.0
google-genai>=0.6.0

