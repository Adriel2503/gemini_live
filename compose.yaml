services:
  gemini-live-web:
    build:
      context: .
    # Variables desde .env (GEMINI_API_KEY, GEMINI_MODEL, etc.)
    env_file:
      - .env
    # El servidor escucha en el puerto 8000 dentro del contenedor.
    ports:
      - "8000:8000"
    restart: unless-stopped
